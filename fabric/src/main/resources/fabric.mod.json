{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",

  "name": "${mod_name}",
  "description": "${description}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {},

  "license": "MIT",

  "environment": "*",
  "entrypoints": {
    "main": [],
    "client": [
      "com.telepathicgrunt.structurevoidtoggle.fabric.StructureVoidToggleClient"
    ]
  },
  "mixins": [
    "${mod_id}.mixins.json",
    "${mod_id}_fabric.mixins.json"
  ],

  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": "*",
    "minecraft": ">=${minecraft_version}"
  }
}
